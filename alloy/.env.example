# Grafana Cloud credentials for Alloy
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# DO NOT commit .env to git (it's in .gitignore)
#
# These values are used by docker-compose.yml to inject credentials
# into config.alloy.tpl at container startup.
#
# You can find these values in the Grafana Cloud portal under
# your stack's configuration, or from the Alloy install command
# provided by Grafana Cloud.

# Loki endpoint for logs (GCLOUD_HOSTED_LOGS_URL)
GRAFANA_CLOUD_LOGS_URL=

# Loki instance ID (GCLOUD_HOSTED_LOGS_ID)
GRAFANA_CLOUD_LOGS_ID=

# Prometheus endpoint for metrics (GCLOUD_HOSTED_METRICS_URL)
GRAFANA_CLOUD_METRICS_URL=

# Prometheus instance ID (GCLOUD_HOSTED_METRICS_ID)
GRAFANA_CLOUD_METRICS_ID=

# Grafana Cloud API key (GCLOUD_RW_API_KEY) - shared by both logs and metrics
GRAFANA_CLOUD_API_KEY=
